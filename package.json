{
  "name": "mcp-hub",
  "version": "1.0.0",
  "description": "A self-updating hub page listing every free MCP plugin - discovers them from GitHub topics.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260424.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.105.0"
  },
  "overrides": {
    "@cloudflare/workers-types": "^4.20260424.1"
  }
}
